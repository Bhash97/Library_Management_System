<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a5012_library_management_system">
    
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibraryManagement">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="BookAddActivity" />
        <activity android:name="BookViewActivity" />
        <activity android:name="MemberAddActivity" />
        <activity android:name="PublisherActivity" />
        <activity android:name="AuthorAddActivity" />
        <activity android:name="BranchAddActivity" />
    </application>
</manifest>
